# 2026-02-17

## Klaviyo Segment Automation
- Built `scripts/klaviyo-setup-segments.sh` — creates 20 standard segments (11 Essential + 4 Exclusion + 5 Advanced) per client using their Klaviyo Private API Key
- Auto-discovers metric IDs (Placed Order, Viewed Product, etc.)
- Cron `21e08c57` (daily 9am) checks Airtable for clients needing setup (no Klaviyo Segments Done flag)
- Airtable fields added: "Klaviyo API Key" (text), "Klaviyo Segments Done" (checkbox)
- Tested against Snocks GmbH (key `TZE6mb`) — **failed with URL encoding bug**
- Bug: `klav()` function's embedded Python passes unencoded filter params (spaces in `equals(name,'Placed Order')`) → Python `http.client` rejects. Fix needed: URL-encode query params with `%20`

## Shopify OAuth → Manual Token
- OAuth abandoned after multiple failed approaches (n8n webhook, local Node.js + Cloudflare tunnel, server deploy)
- Decision: manual Custom App token creation during onboarding call
- Guide created: `docs/shopify-token-anleitung.md`
- n8n Shopify workflow deleted, local OAuth files removed

## n8n Production Webhooks
- Confirmed: production webhooks do NOT register reliably even after UI toggle + restart
- Only formTrigger nodes work — standard webhook nodes fail silently
- All webhook-dependent features switched to polling/cron or formTrigger

## Cron Updates
- All cron sub-agents updated to NOT use `message send` (causes error in isolated sessions)
- Contract Signed Check (`09f6c583`) paused — no active leads
- Contract Reminder (`23b19edb`) enabled
- Onboarding Reminder (`167012a3`) enabled

## Telegram Bot
- Token updated `[REDACTED]`
- n8n API key also refreshed

## Klaviyo Script Fix
- URL-Encoding Bug gefixt: `klav()` nutzt jetzt `urllib.parse.quote()` für Query-Params
- Bash→Python Variable-Passing gefixt: env vars statt heredoc `$VAR`
- Idempotenz: existierende Segmente werden vor Create geprüft
- Alle Metrics werden in einem paginierten Call geholt statt einzeln gefiltert
- Snocks Klaviyo Key `TZE6mb` ist ungültig (zu kurz) — braucht echten Private API Key

## Notion Integration
- API Key: `[REDACTED - siehe ~/.config/notion/api_key]`
- Page: "eCom Email Calendar Kit" (`7cdde815-187c-4741-86a8-2f62621e1f00`)
- Database ID: `3465a32b-e5e0-4d52-bec3-c24ff39e1507`
- Data Source ID: `f973c96b-5659-4fd8-97c2-63984dbd89d9`
- 177 Einträge, Spalten: Name (title), Date (date, 2023 als Jahreskalender), Email Type (multi_select: Social Proof, Sale, Education, Promotion, Content, Survey)
- Jeder Eintrag hat Page Content mit Bullet-Points/Tipps

## Newsletter-Automatisierung
- Cron `dbcb229e` "Newsletter: Wöchentliche Vorbereitung" — Montag 9 Uhr
- Script: `scripts/notion-weekly-newsletters.sh` — holt Notion-Themen der aktuellen Woche (matched Month+Day von 2023→aktuelles Jahr)
- Flow: Notion Topics → Client Website analysieren → Newsletter-Draft generieren → Klaviyo Campaign Draft erstellen → Report
- Nur als DRAFT — Deniz reviewt und sendet manuell

## Next Steps
- Snocks braucht echten Klaviyo Private API Key
- Deniz's signature for contract pre-signing still pending
- Switch Stripe to live key when ready
